{{! Entrega de Documento - Matrizadores }}
{{! layout: 'matrizador' }}
<div class="container mt-4">
  <h2>Entrega de Documento (Matrizadores)</h2>
  <div class="alert alert-info">Aquí se mostrará la interfaz para entregar un documento como matrizador.</div>
</div>

<div class="row">
  <div class="col-md-6 mx-auto">
    <div class="card">
      <div class="card-header">
        <i class="fas fa-hand-holding me-2"></i> Entrega de Documentos
      </div>
      <div class="card-body">
        {{#if documento}}
          <!-- Modo: Verificación de documento específico -->
          <h5 class="mb-4">Verificación para Entrega</h5>
          
          {{! ============== LÓGICA CONDICIONAL: CON O SIN CÓDIGO DE VERIFICACIÓN ============== }}
          {{#if documento.codigoVerificacion}}
            {{! Documento CON código de verificación - Interfaz tradicional }}
            <div class="alert alert-info">
              <div class="d-flex align-items-center">
                <i class="fas fa-info-circle fa-2x me-3"></i>
                <div>
                  <p class="mb-0">El cliente debe proporcionar el código de verificación de 4 dígitos que recibió por teléfono o correo electrónico.</p>
                </div>
              </div>
            </div>
          {{else}}
            {{! Documento SIN código de verificación - Métodos alternativos }}
            <div class="alert alert-warning">
              <div class="d-flex align-items-center">
                <i class="fas fa-exclamation-triangle fa-2x me-3"></i>
                <div>
                  <h6 class="mb-1">Documento sin código de verificación</h6>
                  <p class="mb-0">Este documento no tiene código de verificación. Use uno de los métodos alternativos de verificación.</p>
                </div>
              </div>
            </div>
          {{/if}}
          
          <div class="row mb-4">
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label">Documento:</label>
                <div class="form-control bg-light">{{documento.tipoDocumento}}</div>
              </div>
            </div>
            <div class="col-md-6">
              <div class="mb-3">
                <label class="form-label">Código de Barras:</label>
                <div class="form-control bg-light">{{documento.codigoBarras}}</div>
              </div>
            </div>
            <div class="col-md-12">
              <div class="mb-0">
                <label class="form-label">Cliente:</label>
                <div class="form-control bg-light">
                  {{documento.nombreCliente}} ({{documento.identificacionCliente}})
                </div>
              </div>
            </div>
          </div>

          {{! ============== NUEVA FUNCIONALIDAD: ALERTAS DE ENTREGA GRUPAL PARA MATRIZADORES ============== }}
          {{#if documentosGrupales}}
            {{#if (gt documentosGrupales.documentosPropiios.cantidad 0)}}
            <div class="alert alert-info border-left-info mb-4" id="alertaEntregaGrupalMatrizador">
              <div class="row align-items-center">
                <div class="col-md-8">
                  <h6 class="alert-heading mb-1">
                    <i class="fas fa-user-check text-info me-2"></i>
                    <strong>👤 Sus Documentos Adicionales</strong>
                  </h6>
                  <p class="mb-2">
                    Puede agrupar <strong>{{documentosGrupales.documentosPropiios.cantidad}} documento(s) más</strong> 
                    asignado(s) a usted para este cliente.
                  </p>
                  <small class="text-muted">
                    ✅ Documentos validados de su asignación | 🔐 Estado: Listo | 💰 Pago: Completo
                  </small>
                </div>
                <div class="col-md-4 text-end">
                  <button type="button" class="btn btn-primary btn-sm me-1" onclick="mostrarEntregaGrupalMatrizador()">
                    <i class="fas fa-boxes me-1"></i> Agrupar Mis Documentos
                  </button>
                  <button type="button" class="btn btn-outline-secondary btn-sm" onclick="ocultarAlertaGrupalMatrizador()">
                    <i class="fas fa-file me-1"></i> Solo Este
                  </button>
                </div>
              </div>
              
              {{! Detalles de documentos propios }}
              <div class="mt-3" id="detallesDocumentosPropiios" style="display: none;">
                <h6><i class="fas fa-list me-2"></i>Sus documentos adicionales:</h6>
                <div class="table-responsive">
                  <table class="table table-sm table-bordered">
                    <thead class="table-light">
                      <tr>
                        <th>Código</th>
                        <th>Tipo</th>
                        <th>Valor</th>
                        <th>Estado Pago</th>
                      </tr>
                    </thead>
                    <tbody>
                      {{#each documentosGrupales.documentosPropiios.documentos}}
                      <tr>
                        <td><small>{{this.codigoBarras}}</small></td>
                        <td>{{this.tipoDocumento}}</td>
                        <td>${{#if this.valorFactura}}{{formatMoney this.valorFactura}}{{else}}0.00{{/if}}</td>
                        <td>
                          {{#if (eq this.estadoPago "pagado_completo")}}
                          <span class="badge bg-success">Completo</span>
                          {{else if (eq this.estadoPago "pagado_con_retencion")}}
                          <span class="badge bg-info">Con Retención</span>
                          {{/if}}
                        </td>
                      </tr>
                      {{/each}}
                    </tbody>
                  </table>
                </div>
                <div class="alert alert-info alert-sm mt-2">
                  <i class="fas fa-info-circle me-1"></i>
                  <strong>Matrizador:</strong> Solo puede procesar documentos de su asignación en entrega grupal.
                </div>
              </div>
            </div>
            {{/if}}
            
            {{#if (gt documentosGrupales.documentosOtros.cantidad 0)}}
            <div class="alert alert-warning border-left-warning mb-4">
              <div class="row align-items-center">
                <div class="col-md-8">
                  <h6 class="alert-heading mb-1">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                    <strong>⚠️ Documentos de Otros Matrizadores</strong>
                  </h6>
                  <p class="mb-2">
                    Este cliente tiene <strong>{{documentosGrupales.documentosOtros.cantidad}} documento(s) más</strong> 
                    con otros matrizadores que también están listos para entrega.
                  </p>
                  <small class="text-muted">
                    📋 No puede procesarlos desde aquí | 🏢 Dirigir cliente a Recepción
                  </small>
                </div>
                <div class="col-md-4 text-end">
                  <button type="button" class="btn btn-outline-warning btn-sm" onclick="mostrarDocumentosOtros()">
                    <i class="fas fa-eye me-1"></i> Ver Detalles
                  </button>
                </div>
              </div>
              
              {{! Detalles de documentos de otros matrizadores }}
              <div class="mt-3" id="detallesDocumentosOtros" style="display: none;">
                <h6><i class="fas fa-users me-2"></i>Documentos de otros matrizadores:</h6>
                <div class="table-responsive">
                  <table class="table table-sm table-bordered">
                    <thead class="table-light">
                      <tr>
                        <th>Código</th>
                        <th>Tipo</th>
                        <th>Matrizador</th>
                        <th>Valor</th>
                      </tr>
                    </thead>
                    <tbody>
                      {{#each documentosGrupales.documentosOtros.documentos}}
                      <tr>
                        <td><small>{{this.codigoBarras}}</small></td>
                        <td>{{this.tipoDocumento}}</td>
                        <td><strong>{{this.matrizador.nombre}}</strong></td>
                        <td>${{#if this.valorFactura}}{{formatMoney this.valorFactura}}{{else}}0.00{{/if}}</td>
                      </tr>
                      {{/each}}
                    </tbody>
                  </table>
                </div>
                <div class="alert alert-warning alert-sm mt-2">
                  <i class="fas fa-info-circle me-1"></i>
                  <strong>Importante:</strong> Dirigir al cliente a <strong>Recepción</strong> para retirar estos documentos junto con el suyo.
                </div>
              </div>
            </div>
            {{/if}}
          {{/if}}
          
          <form id="formVerificacion" action="/matrizador/documentos/completar-entrega/{{documento.id}}" method="POST">
            {{#if documento.codigoVerificacion}}
              {{! ============== INTERFAZ PARA DOCUMENTOS CON CÓDIGO ============== }}
              <!-- Sección de verificación -->
              <div class="card bg-light mb-4">
                <div class="card-body">
                  <h6 class="card-title">Verificación de Código</h6>
                  
                  <div class="form-group mb-3">
                    <label for="codigoVerificacion" class="form-label">Código de 4 dígitos:</label>
                    <div class="input-group">
                      <input type="text" class="form-control form-control-lg text-center" id="codigoVerificacion" name="codigoVerificacion" 
                             maxlength="4" inputmode="numeric" pattern="[0-9]{4}" placeholder="0000" required
                             style="letter-spacing: 0.5em; font-weight: bold;">
                      <button class="btn btn-primary" type="submit">
                        <i class="fas fa-check-circle me-1"></i> Verificar
                      </button>
                    </div>
                    {{#if error}}
                    <div class="text-danger mt-2">
                      <i class="fas fa-exclamation-circle me-1"></i> {{error}}
                    </div>
                    {{/if}}
                  </div>
                  
                  <div class="text-center mt-2">
                    <button type="button" class="btn btn-link" data-bs-toggle="modal" data-bs-target="#verificacionLlamadaModal">
                      <i class="fas fa-phone me-1"></i> El cliente no recuerda el código
                    </button>
                  </div>
                </div>
              </div>
            {{else}}
              {{! ============== INTERFAZ PARA DOCUMENTOS SIN CÓDIGO ============== }}
              <!-- Sección de verificación alternativa -->
              <div class="card bg-warning bg-opacity-10 mb-4">
                <div class="card-body">
                  <h6 class="card-title">
                    <i class="fas fa-shield-alt me-2"></i>Métodos de Verificación Alternativos
                  </h6>
                  
                  <div class="form-group mb-3">
                    <label class="form-label">Seleccione el método de verificación:</label>
                    <div class="row">
                      <div class="col-md-4 mb-2">
                        <div class="form-check">
                          <input class="form-check-input" type="radio" name="tipoVerificacion" id="verificacionIdentidad" value="identidad" required>
                          <label class="form-check-label" for="verificacionIdentidad">
                            <i class="fas fa-id-card me-1"></i> Cédula de Identidad
                          </label>
                        </div>
                      </div>
                      <div class="col-md-4 mb-2">
                        <div class="form-check">
                          <input class="form-check-input" type="radio" name="tipoVerificacion" id="verificacionFactura" value="factura" required>
                          <label class="form-check-label" for="verificacionFactura">
                            <i class="fas fa-receipt me-1"></i> Número de Factura
                          </label>
                        </div>
                      </div>
                      <div class="col-md-4 mb-2">
                        <div class="form-check">
                          <input class="form-check-input" type="radio" name="tipoVerificacion" id="verificacionLlamada" value="llamada" required>
                          <label class="form-check-label" for="verificacionLlamada">
                            <i class="fas fa-phone me-1"></i> Llamada Telefónica
                          </label>
                        </div>
                      </div>
                    </div>
                  </div>
                  
                  <div class="form-group mb-3">
                    <label for="observaciones" class="form-label">Detalles de la verificación:</label>
                    <textarea class="form-control" id="observaciones" name="observaciones" rows="3" 
                              placeholder="Describa los detalles del método de verificación utilizado..." required></textarea>
                    <div class="form-text">
                      <strong>Ejemplos:</strong><br>
                      • <em>Cédula:</em> "Verificada cédula 1234567890 coincide con cliente"<br>
                      • <em>Factura:</em> "Verificado número de factura 001-001-000123456"<br>
                      • <em>Llamada:</em> "Llamé al 0987654321, confirmó datos personales y trámite"
                    </div>
                  </div>
                  
                  {{#if error}}
                  <div class="alert alert-danger">
                    <i class="fas fa-exclamation-circle me-1"></i> {{error}}
                  </div>
                  {{/if}}
                </div>
              </div>
            {{/if}}
            
            <!-- Sección de datos del receptor -->
            <div class="card mb-4">
              <div class="card-body">
                <h6 class="card-title">Datos de quien recibe</h6>
                
                <div class="row">
                  <div class="col-md-6 mb-3">
                    <label for="nombreReceptor" class="form-label">Nombre completo:</label>
                    <input type="text" class="form-control" id="nombreReceptor" name="nombreReceptor" required>
                  </div>
                  
                  <div class="col-md-6 mb-3">
                    <label for="identificacionReceptor" class="form-label">Identificación:</label>
                    <input type="text" class="form-control" id="identificacionReceptor" name="identificacionReceptor" required>
                  </div>
                  
                  <div class="col-md-12 mb-0">
                    <label for="relacionReceptor" class="form-label">Relación con el cliente:</label>
                    <select class="form-select" id="relacionReceptor" name="relacionReceptor" required>
                      <option value="" selected disabled>Seleccionar...</option>
                      <option value="titular">Titular del documento</option>
                      <option value="familiar">Familiar</option>
                      <option value="mandatario">Mandatario</option>
                      <option value="otro">Otro</option>
                    </select>
                  </div>
                </div>
              </div>
            </div>
            
            {{! ============== CAMPOS OCULTOS PARA ENTREGA GRUPAL ============== }}
            <input type="hidden" id="entregaGrupal" name="entregaGrupal" value="false">
            <input type="hidden" id="documentosAdicionales" name="documentosAdicionales" value="">
            <input type="hidden" id="tipoEntregaGrupal" name="tipoEntregaGrupal" value="{{tipoEntrega}}">
            
            <div class="d-grid">
              <button type="submit" class="btn btn-primary btn-lg" id="btnCompletarEntrega">
                <i class="fas fa-paper-plane me-1"></i> Completar Entrega
              </button>
            </div>
          </form>
        {{else}}
          <!-- Modo: Búsqueda de documento para entrega -->
          <h5 class="mb-4">Buscar Documento para Entrega</h5>
          
          <div class="alert alert-info mb-4">
            <div class="d-flex align-items-center">
              <i class="fas fa-info-circle fa-2x me-3"></i>
              <div>
                <p class="mb-0">Ingrese el código de barras o busque un documento listo para entrega.</p>
              </div>
            </div>
          </div>
          
          <form action="/matrizador/documentos/entrega" method="GET">
            <div class="input-group mb-4">
              <input type="text" class="form-control" name="codigo" placeholder="Escanear o ingresar código de barras">
              <button class="btn btn-primary" type="submit">
                <i class="fas fa-search me-1"></i> Buscar
              </button>
            </div>
          </form>
          
          <h6 class="mb-3 border-bottom pb-2">Mis Documentos Listos para Entrega</h6>
          
          <div class="list-group">
            {{#each documentosListos}}
            <a href="/matrizador/documentos/entrega/{{this.id}}" class="list-group-item list-group-item-action">
              <div class="d-flex w-100 justify-content-between">
                <h6 class="mb-1">{{this.tipoDocumento}}</h6>
                <small class="fecha-documento-entrega" data-fecha="{{this.updatedAt}}">
                  Cargando...
                </small>
              </div>
              <p class="mb-1">{{this.nombreCliente}}</p>
              <small>Código: {{this.codigoBarras}}</small>
            </a>
            {{else}}
            <div class="text-center py-4">
              <i class="fas fa-info-circle me-2"></i> No hay documentos listos para entrega
            </div>
            {{/each}}
          </div>
        {{/if}}
      </div>
    </div>
  </div>
</div>

{{! ============== MODAL DE CONFIRMACIÓN ENTREGA GRUPAL MATRIZADORES ============== }}
{{#if (and documentosGrupales.documentosPropiios (gt documentosGrupales.documentosPropiios.cantidad 0))}}
<div class="modal fade" id="modalEntregaGrupalMatrizador" tabindex="-1" aria-labelledby="modalEntregaGrupalMatrizadorLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title" id="modalEntregaGrupalMatrizadorLabel">
          <i class="fas fa-user-check me-2"></i>Confirmar Entrega Grupal - Sus Documentos
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="alert alert-info">
          <h6><i class="fas fa-shield-check me-2"></i>Validaciones de Seguridad Completadas</h6>
          <ul class="mb-0">
            <li>✅ Todos los documentos están asignados a usted</li>
            <li>✅ Estado "Listo para entrega" confirmado</li>
            <li>✅ Pagos completados (completo o con retención)</li>
            <li>✅ Sin entregas previas registradas</li>
            <li>✅ Pertenecen al mismo cliente</li>
          </ul>
        </div>
        
        <h6><i class="fas fa-file-alt me-2"></i>Documento Principal:</h6>
        <div class="card mb-3">
          <div class="card-body p-2">
            <strong>{{documento.codigoBarras}}</strong> - {{documento.tipoDocumento}}
            <br><small class="text-muted">{{documento.nombreCliente}}</small>
          </div>
        </div>
        
        <h6><i class="fas fa-plus-circle me-2"></i>Sus Documentos Adicionales ({{documentosGrupales.documentosPropiios.cantidad}}):</h6>
        <div class="table-responsive mb-3">
          <table class="table table-sm">
            <thead class="table-light">
              <tr>
                <th>Código</th>
                <th>Tipo</th>
                <th>Valor</th>
                <th>Estado Pago</th>
              </tr>
            </thead>
            <tbody>
              {{#each documentosGrupales.documentosPropiios.documentos}}
              <tr>
                <td><small>{{this.codigoBarras}}</small></td>
                <td>{{this.tipoDocumento}}</td>
                <td>${{#if this.valorFactura}}{{formatMoney this.valorFactura}}{{else}}0.00{{/if}}</td>
                <td>
                  {{#if (eq this.estadoPago "pagado_completo")}}
                  <span class="badge bg-success">Completo</span>
                  {{else if (eq this.estadoPago "pagado_con_retencion")}}
                  <span class="badge bg-info">Con Retención</span>
                  {{/if}}
                </td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
        
        <div class="alert alert-success">
          <i class="fas fa-info-circle me-2"></i>
          <strong>Matrizador:</strong> Solo puede procesar documentos de su asignación en entrega grupal.
        </div>
        
        <div class="alert alert-warning">
          <i class="fas fa-exclamation-triangle me-2"></i>
          <strong>Importante:</strong> Se entregarán <strong>{{add documentosGrupales.documentosPropiios.cantidad 1}} documentos</strong> en total al receptor especificado.
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
          <i class="fas fa-times me-1"></i> Cancelar
        </button>
        <button type="button" class="btn btn-primary" onclick="confirmarEntregaGrupalMatrizador()">
          <i class="fas fa-boxes me-1"></i> Confirmar Entrega Grupal
        </button>
      </div>
    </div>
  </div>
</div>
{{/if}}

{{#if documento.codigoVerificacion}}
<!-- Modal de verificación por llamada (solo para documentos CON código) -->
<div class="modal fade" id="verificacionLlamadaModal" tabindex="-1" aria-labelledby="verificacionLlamadaModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="verificacionLlamadaModalLabel">Verificación Alternativa por Llamada</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="alert alert-warning">
          <i class="fas fa-exclamation-triangle me-2"></i> Este método solo debe usarse cuando el cliente no recuerda el código y es necesario verificar su identidad por teléfono.
        </div>
        
        <div class="mb-3">
          <label class="form-label">Cliente:</label>
          <div class="form-control bg-light">{{documento.nombreCliente}} ({{documento.identificacionCliente}})</div>
        </div>
        
        <div class="mb-3">
          <label class="form-label">Teléfono de contacto:</label>
          <div class="form-control bg-light">{{documento.telefonoCliente}}</div>
        </div>
        
        <div class="form-check mb-3">
          <input class="form-check-input" type="checkbox" id="confirmacionLlamada">
          <label class="form-check-label" for="confirmacionLlamada">
            Confirmo que he verificado la identidad del cliente por teléfono.
          </label>
        </div>
        
        <div class="mb-3">
          <label class="form-label">Observaciones:</label>
          <textarea class="form-control" id="observacionesLlamada" rows="3" placeholder="Detalles de la verificación realizada..."></textarea>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
        <button type="button" class="btn btn-primary" id="confirmarVerificacionLlamada" disabled>
          <i class="fas fa-check me-1"></i> Confirmar Verificación
        </button>
      </div>
    </div>
  </div>
</div>
{{/if}}

<script>
  document.addEventListener('DOMContentLoaded', function() {
    {{#if documento.codigoVerificacion}}
      // ============== SCRIPT PARA DOCUMENTOS CON CÓDIGO ==============
      // Auto-focus en el campo de código
      const codigoInput = document.getElementById('codigoVerificacion');
      if (codigoInput) {
        codigoInput.focus();
        
        // Solo permitir dígitos
        codigoInput.addEventListener('input', function(e) {
          this.value = this.value.replace(/[^0-9]/g, '');
        });
        
        // Submit del formulario cuando se complete el código
        codigoInput.addEventListener('input', function() {
          if (this.value.length === 4) {
            // Dar tiempo para verificar visualmente
            setTimeout(function() {
              document.getElementById('nombreReceptor').focus();
            }, 300);
          }
        });
      }
      
      // Verificación por llamada
      const checkboxConfirmacion = document.getElementById('confirmacionLlamada');
      const btnConfirmarLlamada = document.getElementById('confirmarVerificacionLlamada');
      
      if (checkboxConfirmacion && btnConfirmarLlamada) {
        checkboxConfirmacion.addEventListener('change', function() {
          btnConfirmarLlamada.disabled = !this.checked;
        });
        
        btnConfirmarLlamada.addEventListener('click', function() {
          const observaciones = document.getElementById('observacionesLlamada').value;
          
          // Preparar formulario para envío
          const form = document.getElementById('formVerificacion');
          
          // Agregar campo oculto para indicar verificación por llamada
          const inputTipoVerificacion = document.createElement('input');
          inputTipoVerificacion.type = 'hidden';
          inputTipoVerificacion.name = 'tipoVerificacion';
          inputTipoVerificacion.value = 'llamada';
          form.appendChild(inputTipoVerificacion);
          
          // Agregar observaciones
          const inputObservaciones = document.createElement('input');
          inputObservaciones.type = 'hidden';
          inputObservaciones.name = 'observaciones';
          inputObservaciones.value = observaciones;
          form.appendChild(inputObservaciones);
          
          // Cerrar modal y enviar formulario
          const modal = bootstrap.Modal.getInstance(document.getElementById('verificacionLlamadaModal'));
          modal.hide();
          form.submit();
        });
      }
    {{else}}
      // ============== SCRIPT PARA DOCUMENTOS SIN CÓDIGO ==============
      // Validación de métodos alternativos
      const tiposVerificacion = document.querySelectorAll('input[name="tipoVerificacion"]');
      const observacionesTextarea = document.getElementById('observaciones');
      
      // Actualizar placeholder según el tipo seleccionado
      tiposVerificacion.forEach(radio => {
        radio.addEventListener('change', function() {
          if (this.checked) {
            switch(this.value) {
              case 'identidad':
                observacionesTextarea.placeholder = 'Ej: Verificada cédula 1234567890, coincide con el nombre del cliente y datos del documento';
                break;
              case 'factura':
                observacionesTextarea.placeholder = 'Ej: Cliente presentó factura número 001-001-000123456, coincide con el documento';
                break;
              case 'llamada':
                observacionesTextarea.placeholder = 'Ej: Llamé al 0987654321, cliente confirmó nombre completo, cédula y detalles del trámite';
                break;
            }
          }
        });
      });
      
      // Auto-focus en el primer método de verificación
      if (tiposVerificacion.length > 0) {
        tiposVerificacion[0].focus();
      }
    {{/if}}

    // ============== FUNCIONES PARA ENTREGA GRUPAL - MATRIZADORES ==============
    
    // Mostrar modal de entrega grupal para matrizador
    window.mostrarEntregaGrupalMatrizador = function() {
      // Validar que se hayan completado los datos del receptor
      const nombreReceptor = document.getElementById('nombreReceptor').value;
      const identificacionReceptor = document.getElementById('identificacionReceptor').value;
      const relacionReceptor = document.getElementById('relacionReceptor').value;
      
      if (!nombreReceptor || !identificacionReceptor || !relacionReceptor) {
        alert('⚠️ Debe completar todos los datos del receptor antes de proceder con entrega grupal');
        return;
      }
      
      // Mostrar detalles de documentos propios
      document.getElementById('detallesDocumentosPropiios').style.display = 'block';
      
      // Mostrar modal de confirmación
      const modal = new bootstrap.Modal(document.getElementById('modalEntregaGrupalMatrizador'));
      modal.show();
    };
    
    // Confirmar entrega grupal para matrizador
    window.confirmarEntregaGrupalMatrizador = function() {
      // Obtener IDs de documentos propios
      const documentosIds = [
        {{#each documentosGrupales.documentosPropiios.documentos}}
        {{this.id}}{{#unless @last}},{{/unless}}
        {{/each}}
      ];
      
      if (documentosIds.length === 0) {
        alert('❌ No hay documentos adicionales para procesar');
        return;
      }
      
      // Configurar campos ocultos
      document.getElementById('entregaGrupal').value = 'true';
      document.getElementById('documentosAdicionales').value = documentosIds.join(',');
      document.getElementById('tipoEntregaGrupal').value = 'matrizador_limitada';
      
      // Actualizar texto del botón
      const btnCompletar = document.getElementById('btnCompletarEntrega');
      btnCompletar.innerHTML = '<i class="fas fa-boxes me-2"></i> Completar Entrega Grupal (' + (documentosIds.length + 1) + ' documentos)';
      btnCompletar.classList.remove('btn-primary');
      btnCompletar.classList.add('btn-success');
      
      // Cerrar modal
      const modal = bootstrap.Modal.getInstance(document.getElementById('modalEntregaGrupalMatrizador'));
      modal.hide();
      
      // Mostrar confirmación
      const alertaGrupal = document.getElementById('alertaEntregaGrupalMatrizador');
      alertaGrupal.classList.remove('alert-info');
      alertaGrupal.classList.add('alert-success');
      alertaGrupal.innerHTML = `
        <div class="row align-items-center">
          <div class="col-md-8">
            <h6 class="alert-heading mb-1">
              <i class="fas fa-boxes text-success me-2"></i>
              <strong>👤 Entrega Grupal Activada</strong>
            </h6>
            <p class="mb-0">
              Se procesarán <strong>${documentosIds.length + 1} documentos</strong> de su asignación en entrega grupal.
              <br><small class="text-muted">Haga clic en "Completar Entrega Grupal" para procesar todos sus documentos.</small>
            </p>
          </div>
          <div class="col-md-4 text-end">
            <button type="button" class="btn btn-outline-secondary btn-sm" onclick="cancelarEntregaGrupalMatrizador()">
              <i class="fas fa-times me-1"></i> Cancelar Grupal
            </button>
          </div>
        </div>
      `;
      
      console.log('✅ [MATRIZADOR] Entrega grupal configurada:', documentosIds.length + 1, 'documentos propios');
    };
    
    // Mostrar documentos de otros matrizadores
    window.mostrarDocumentosOtros = function() {
      const detalles = document.getElementById('detallesDocumentosOtros');
      if (detalles.style.display === 'none') {
        detalles.style.display = 'block';
      } else {
        detalles.style.display = 'none';
      }
    };
    
    // Ocultar alerta de entrega grupal
    window.ocultarAlertaGrupalMatrizador = function() {
      document.getElementById('alertaEntregaGrupalMatrizador').style.display = 'none';
    };
    
    // Cancelar entrega grupal
    window.cancelarEntregaGrupalMatrizador = function() {
      // Resetear campos ocultos
      document.getElementById('entregaGrupal').value = 'false';
      document.getElementById('documentosAdicionales').value = '';
      
      // Restaurar botón original
      const btnCompletar = document.getElementById('btnCompletarEntrega');
      btnCompletar.innerHTML = '<i class="fas fa-paper-plane me-1"></i> Completar Entrega';
      btnCompletar.classList.remove('btn-success');
      btnCompletar.classList.add('btn-primary');
      
      // Restaurar alerta original
      location.reload(); // Recargar para restaurar estado original
    };
    
    // Validación antes de envío del formulario
    document.getElementById('formVerificacion').addEventListener('submit', function(e) {
      const entregaGrupal = document.getElementById('entregaGrupal').value;
      const tipoEntrega = document.getElementById('tipoEntregaGrupal').value;
      
      if (entregaGrupal === 'true') {
        console.log('📋 [MATRIZADOR] Enviando entrega grupal:', tipoEntrega);
        
        // Validar que todos los campos estén completos
        const nombreReceptor = document.getElementById('nombreReceptor').value;
        const identificacionReceptor = document.getElementById('identificacionReceptor').value;
        const relacionReceptor = document.getElementById('relacionReceptor').value;
        
        if (!nombreReceptor || !identificacionReceptor || !relacionReceptor) {
          e.preventDefault();
          alert('❌ Debe completar todos los datos del receptor para entrega grupal');
          return false;
        }
        
        // Confirmar entrega grupal
        if (!confirm(`¿Confirma la entrega grupal de ${document.getElementById('documentosAdicionales').value.split(',').length + 1} documentos de su asignación?`)) {
          e.preventDefault();
          return false;
        }
      }
      
      return true;
    });
    
    // ============== FORMATEAR FECHAS DE DOCUMENTOS ==============
    console.log('🕒 Iniciando formateo de fechas en entrega de documentos...');
    
    // Función para formatear fecha de manera robusta
    function formatearFechaSegura(fechaRaw) {
      if (!fechaRaw || fechaRaw === 'null' || fechaRaw === '' || fechaRaw === 'undefined') {
        return 'Sin fecha';
      }
      
      try {
        // Intentar diferentes formatos de fecha
        let fecha;
        
        // Si ya es un objeto Date
        if (fechaRaw instanceof Date) {
          fecha = fechaRaw;
        }
        // Si es un string ISO
        else if (typeof fechaRaw === 'string') {
          // Limpiar la fecha de caracteres extraños
          const fechaLimpia = fechaRaw.trim();
          fecha = new Date(fechaLimpia);
        }
        // Si es un timestamp
        else if (typeof fechaRaw === 'number') {
          fecha = new Date(fechaRaw);
        }
        else {
          console.warn('Formato de fecha no reconocido:', fechaRaw);
          return 'Formato inválido';
        }
        
        // Verificar si la fecha es válida
        if (isNaN(fecha.getTime())) {
          console.warn('Fecha inválida después de conversión:', fechaRaw);
          return 'Fecha inválida';
        }
        
        return fecha;
      } catch (error) {
        console.error('Error procesando fecha:', error, 'Fecha original:', fechaRaw);
        return 'Error en fecha';
      }
    }
    
    // Formatear fechas de documentos en entrega
    document.querySelectorAll('.fecha-documento-entrega').forEach(function(elemento) {
      const fechaRaw = elemento.getAttribute('data-fecha');
      console.log('📅 Procesando fecha de documento en entrega:', fechaRaw);
      
      const fechaFormateada = formatearFechaSegura(fechaRaw);
      
      if (fechaFormateada instanceof Date) {
        elemento.textContent = fechaFormateada.toLocaleDateString('es-EC', {
          day: '2-digit',
          month: '2-digit', 
          year: 'numeric'
        });
        elemento.classList.add('text-success');
      } else {
        elemento.textContent = fechaFormateada;
        elemento.classList.add('text-muted');
      }
    });
    
    console.log('✅ Formateo de fechas de documentos en entrega completado');
  });
</script> 