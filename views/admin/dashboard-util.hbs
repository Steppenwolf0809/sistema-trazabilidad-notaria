<!-- Dashboard Administrativo MODERNIZADO -->

<!-- 🎯 INDICADOR ELEGANTE DE PERÍODO ACTIVO -->
<div class="row mb-4">
  <div class="col-12">
    <div class="alert alert-light border-left-primary shadow-sm">
      <div class="d-flex align-items-center justify-content-between">
        <div class="d-flex align-items-center">
          <i class="fas fa-calendar-day text-primary me-3" style="font-size: 1.2rem;"></i>
          <div>
            <span class="text-muted me-2">Mostrando datos de:</span>
            <strong class="text-primary" id="periodoActivo">{{periodo.periodoTexto}}</strong>
          </div>
        </div>
        <div class="text-end">
          {{#if (gt metricas.totalDocumentos 0)}}
            <small class="text-success">
              <i class="fas fa-check-circle me-1"></i>{{metricas.totalDocumentos}} documentos
            </small>
            <br>
            <small class="text-muted">en este período</small>
          {{else}}
            <small class="text-warning">
              <i class="fas fa-info-circle me-1"></i>Sin actividad
            </small>
            <br>
            <small class="text-muted">en este período</small>
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</div>

<!-- 🎛️ FILTROS COMPACTOS Y ACCESIBLES -->
<div class="row mb-4">
  <div class="col-12">
    <div class="card shadow-sm">
      <div class="card-body py-3">
        <form id="periodoForm" class="row align-items-center">
          <div class="col-md-3">
            <select id="tipoPeriodo" name="tipoPeriodo" class="form-select form-select-sm">
              <option value="hoy" {{#if periodo.esHoy}}selected{{/if}}>Hoy</option>
              <option value="ayer" {{#if periodo.esAyer}}selected{{/if}}>Ayer</option>
              <option value="semana" {{#if periodo.esSemana}}selected{{/if}}>Esta semana</option>
              <option value="mes" {{#if periodo.esMes}}selected{{/if}}>Este mes</option>
              <option value="ultimo_mes" {{#if periodo.esUltimoMes}}selected{{/if}}>Últimos 30 días</option>
              <option value="personalizado" {{#if periodo.esPersonalizado}}selected{{/if}}>Personalizado</option>
            </select>
          </div>
          
          <div id="fechasPersonalizadas" class="col-md-6" style="{{#unless periodo.esPersonalizado}}display: none;{{/unless}}">
            <div class="row">
              <div class="col-6">
                <input type="date" id="fechaInicio" name="fechaInicio" class="form-control form-control-sm" value="{{periodo.fechaInicio}}" placeholder="Desde">
              </div>
              <div class="col-6">
                <input type="date" id="fechaFin" name="fechaFin" class="form-control form-control-sm" value="{{periodo.fechaFin}}" placeholder="Hasta">
              </div>
            </div>
          </div>
          
          <div class="col-md-3">
            <div class="btn-group btn-group-sm me-2" role="group">
              <button type="button" class="btn btn-outline-primary" data-periodo="hoy">Hoy</button>
              <button type="button" class="btn btn-outline-primary" data-periodo="semana">Semana</button>
              <button type="button" class="btn btn-outline-primary" data-periodo="mes">Mes</button>
              <button type="button" class="btn btn-outline-primary" data-periodo="ultimo_mes">30d</button>
            </div>
            <button type="submit" class="btn btn-primary btn-sm">
              <i class="fas fa-filter me-1"></i> Aplicar
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- 🚨 ALERTAS CRÍTICAS PROMINENTES -->
{{#if alertasCriticas.length}}
<div class="row mb-4">
  <div class="col-12">
    <div class="card border-left-{{#if (eq estadoGeneral 'danger')}}danger{{else if (eq estadoGeneral 'warning')}}warning{{else}}success{{/if}} shadow-sm">
      <div class="card-header bg-transparent">
        <h6 class="mb-0">
          <i class="fas fa-{{#if (eq estadoGeneral 'danger')}}exclamation-triangle text-danger{{else if (eq estadoGeneral 'warning')}}exclamation-circle text-warning{{else}}check-circle text-success{{/if}} me-2"></i>
          Centro de Alertas - {{mensajeEstado}}
        </h6>
      </div>
      <div class="card-body">
        <div class="row">
          {{#each alertasCriticas}}
          <div class="col-md-6 mb-3">
            <div class="alert alert-{{this.tipo}} mb-0">
              <div class="d-flex align-items-center">
                <i class="{{this.icono}} fa-2x me-3"></i>
                <div class="flex-grow-1">
                  <h6 class="alert-heading mb-1">{{this.titulo}}</h6>
                  <p class="mb-2">{{this.descripcion}}</p>
                  <a href="{{this.accion}}" class="btn btn-sm btn-{{this.tipo}}">
                    <i class="fas fa-arrow-right me-1"></i> Ver Detalles
                  </a>
                </div>
              </div>
            </div>
          </div>
          {{/each}}
        </div>
      </div>
    </div>
  </div>
</div>
{{/if}}

<!-- 📊 MÉTRICAS PRINCIPALES ORGANIZADAS -->
<div class="row mb-4">
  <!-- Documentos Totales -->
  <div class="col-xl-3 col-md-6 mb-4">
    <div class="card border-left-primary shadow-sm h-100">
      <div class="card-body">
        <div class="row no-gutters align-items-center">
          <div class="col mr-2">
            <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">Documentos Totales</div>
            <div class="h5 mb-0 font-weight-bold text-gray-800">{{metricas.totalDocumentos}}</div>
          </div>
          <div class="col-auto">
            <i class="fas fa-file-alt fa-2x text-gray-300"></i>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- En Proceso -->
  <div class="col-xl-3 col-md-6 mb-4">
    <div class="card border-left-warning shadow-sm h-100">
      <div class="card-body">
        <div class="row no-gutters align-items-center">
          <div class="col mr-2">
            <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">En Proceso</div>
            <div class="h5 mb-0 font-weight-bold text-gray-800">{{metricas.enProceso}}</div>
          </div>
          <div class="col-auto">
            <i class="fas fa-cogs fa-2x text-gray-300"></i>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Listos para Entrega -->
  <div class="col-xl-3 col-md-6 mb-4">
    <div class="card border-left-success shadow-sm h-100">
      <div class="card-body">
        <div class="row no-gutters align-items-center">
          <div class="col mr-2">
            <div class="text-xs font-weight-bold text-success text-uppercase mb-1">Listos para Entrega</div>
            <div class="h5 mb-0 font-weight-bold text-gray-800">{{metricas.listoParaEntrega}}</div>
          </div>
          <div class="col-auto">
            <i class="fas fa-check-circle fa-2x text-gray-300"></i>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Entregados -->
  <div class="col-xl-3 col-md-6 mb-4">
    <div class="card border-left-info shadow-sm h-100">
      <div class="card-body">
        <div class="row no-gutters align-items-center">
          <div class="col mr-2">
            <div class="text-xs font-weight-bold text-info text-uppercase mb-1">Entregados</div>
            <div class="h5 mb-0 font-weight-bold text-gray-800">{{metricas.entregados}}</div>
          </div>
          <div class="col-auto">
            <i class="fas fa-hand-holding fa-2x text-gray-300"></i>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- 💰 MÉTRICAS FINANCIERAS -->
<div class="row mb-4">
  <!-- Ingresos del Período -->
  <div class="col-xl-3 col-md-6 mb-4">
    <div class="card border-left-success shadow-sm h-100">
      <div class="card-body">
        <div class="row no-gutters align-items-center">
          <div class="col mr-2">
            <div class="text-xs font-weight-bold text-success text-uppercase mb-1">Ingresos del Período</div>
            <div class="h5 mb-0 font-weight-bold text-gray-800">${{finanzas.ingresosPeriodo}}</div>
            <div class="text-xs text-muted">{{finanzas.documentosCobradosPeriodo}} documentos cobrados</div>
          </div>
          <div class="col-auto">
            <i class="fas fa-dollar-sign fa-2x text-gray-300"></i>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Ingresos de Hoy -->
  <div class="col-xl-3 col-md-6 mb-4">
    <div class="card border-left-info shadow-sm h-100">
      <div class="card-body">
        <div class="row no-gutters align-items-center">
          <div class="col mr-2">
            <div class="text-xs font-weight-bold text-info text-uppercase mb-1">Ingresos de Hoy</div>
            <div class="h5 mb-0 font-weight-bold text-gray-800">${{finanzas.ingresosHoy}}</div>
            <div class="text-xs text-muted">{{finanzas.documentosCobradosHoy}} documentos hoy</div>
          </div>
          <div class="col-auto">
            <i class="fas fa-coins fa-2x text-gray-300"></i>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Facturación del Período -->
  <div class="col-xl-3 col-md-6 mb-4">
    <div class="card border-left-primary shadow-sm h-100">
      <div class="card-body">
        <div class="row no-gutters align-items-center">
          <div class="col mr-2">
            <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">Facturación del Período</div>
            <div class="h5 mb-0 font-weight-bold text-gray-800">${{finanzas.facturacionPeriodo}}</div>
          </div>
          <div class="col-auto">
            <i class="fas fa-file-invoice-dollar fa-2x text-gray-300"></i>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Total Pendiente -->
  <div class="col-xl-3 col-md-6 mb-4">
    <div class="card border-left-warning shadow-sm h-100">
      <div class="card-body">
        <div class="row no-gutters align-items-center">
          <div class="col mr-2">
            <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">Total Pendiente</div>
            <div class="h5 mb-0 font-weight-bold text-gray-800">${{finanzas.totalPendiente}}</div>
            <div class="text-xs text-muted">Por cobrar</div>
          </div>
          <div class="col-auto">
            <i class="fas fa-clock fa-2x text-gray-300"></i>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- 🚀 ACCESOS RÁPIDOS GRANDES Y DESCRIPTIVOS -->
<div class="row mb-4">
  <div class="col-12">
    <div class="card shadow-sm">
      <div class="card-header">
        <h6 class="mb-0">
          <i class="fas fa-bolt text-primary me-2"></i>
          Acciones Rápidas
        </h6>
      </div>
      <div class="card-body">
        <div class="row text-center">
          <div class="col-lg-3 col-md-6 mb-3">
            <a href="/admin/documentos/listado" class="btn btn-lg btn-outline-primary w-100 h-100 py-4">
              <i class="fas fa-list fa-2x mb-3"></i><br>
              <strong>Gestión de Documentos</strong><br>
              <small>Ver y administrar todos los documentos</small>
            </a>
          </div>
          <div class="col-lg-3 col-md-6 mb-3">
            <a href="/admin/reportes/pendientes" class="btn btn-lg btn-outline-danger w-100 h-100 py-4">
              <i class="fas fa-exclamation-triangle fa-2x mb-3"></i><br>
              <strong>Pagos Atrasados</strong><br>
              <small>Gestión de cobranza</small>
            </a>
          </div>
          <div class="col-lg-3 col-md-6 mb-3">
            <a href="/admin/reportes" class="btn btn-lg btn-outline-success w-100 h-100 py-4">
              <i class="fas fa-chart-bar fa-2x mb-3"></i><br>
              <strong>Reportes y Análisis</strong><br>
              <small>Estadísticas detalladas</small>
            </a>
          </div>
          <div class="col-lg-3 col-md-6 mb-3">
            <a href="/admin/matrizadores" class="btn btn-lg btn-outline-info w-100 h-100 py-4">
              <i class="fas fa-users fa-2x mb-3"></i><br>
              <strong>Gestión de Equipo</strong><br>
              <small>Administrar matrizadores</small>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- 👥 RENDIMIENTO DEL EQUIPO -->
{{#if equipoRendimiento.length}}
<div class="row mb-4">
  <div class="col-12">
    <div class="card shadow-sm">
      <div class="card-header">
        <h6 class="mb-0">
          <i class="fas fa-users text-success me-2"></i>
          Rendimiento del Equipo (Últimos 7 días)
        </h6>
      </div>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>Matrizador</th>
                <th>Documentos Procesados</th>
                <th>Documentos Entregados</th>
                <th>Dinero Cobrado</th>
              </tr>
            </thead>
            <tbody>
              {{#each equipoRendimiento}}
              <tr>
                <td>
                  <strong>{{this.nombre}}</strong>
                </td>
                <td>
                  <span class="badge bg-primary">{{this.documentos_procesados}}</span>
                </td>
                <td>
                  <span class="badge bg-success">{{this.documentos_entregados}}</span>
                </td>
                <td>
                  <strong class="text-success">${{this.dinero_cobrado}}</strong>
                </td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
{{/if}}

<!-- 📋 DOCUMENTOS QUE REQUIEREN ATENCIÓN -->
<div class="row mb-4">
  <!-- Documentos Urgentes -->
  <div class="col-md-6">
    <div class="card shadow-sm h-100">
      <div class="card-header bg-danger text-white">
        <h6 class="mb-0">
          <i class="fas fa-exclamation-triangle me-2"></i>
          Documentos Urgentes ({{documentosUrgentes.length}})
        </h6>
      </div>
      <div class="card-body p-0">
        {{#if documentosUrgentes.length}}
          <div class="table-responsive">
            <table class="table table-hover mb-0">
              <thead>
                <tr>
                  <th>Código</th>
                  <th>Cliente</th>
                  <th>Días</th>
                  <th>Matrizador</th>
                </tr>
              </thead>
              <tbody>
                {{#each documentosUrgentes}}
                <tr>
                  <td><code>{{this.codigo_barras}}</code></td>
                  <td>{{this.nombre_cliente}}</td>
                  <td>
                    <span class="badge bg-danger">+15 días</span>
                  </td>
                  <td>{{this.matrizador.nombre}}</td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
        {{else}}
          <div class="text-center py-4">
            <i class="fas fa-check-circle text-success fa-3x mb-3"></i>
            <p class="text-muted mb-0">No hay documentos urgentes</p>
          </div>
        {{/if}}
      </div>
    </div>
  </div>

  <!-- Documentos Listos -->
  <div class="col-md-6">
    <div class="card shadow-sm h-100">
      <div class="card-header bg-success text-white">
        <h6 class="mb-0">
          <i class="fas fa-check-circle me-2"></i>
          Listos para Entrega ({{documentosListos.length}})
        </h6>
      </div>
      <div class="card-body p-0">
        {{#if documentosListos.length}}
          <div class="table-responsive">
            <table class="table table-hover mb-0">
              <thead>
                <tr>
                  <th>Código</th>
                  <th>Cliente</th>
                  <th>Tipo</th>
                  <th>Matrizador</th>
                </tr>
              </thead>
              <tbody>
                {{#each documentosListos}}
                <tr>
                  <td><code>{{this.codigo_barras}}</code></td>
                  <td>{{this.nombre_cliente}}</td>
                  <td>{{this.tipo_documento}}</td>
                  <td>{{this.matrizador.nombre}}</td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
        {{else}}
          <div class="text-center py-4">
            <i class="fas fa-info-circle text-info fa-3x mb-3"></i>
            <p class="text-muted mb-0">No hay documentos listos</p>
          </div>
        {{/if}}
      </div>
    </div>
  </div>
</div>

<!-- 💸 ACTIVIDAD FINANCIERA RECIENTE -->
<div class="row mb-4">
  <!-- Últimos Pagos -->
  <div class="col-md-6">
    <div class="card shadow-sm h-100">
      <div class="card-header">
        <h6 class="mb-0">
          <i class="fas fa-money-bill-wave text-success me-2"></i>
          Últimos Pagos Registrados
        </h6>
      </div>
      <div class="card-body p-0">
        {{#if ultimosPagos.length}}
          <div class="table-responsive">
            <table class="table table-hover mb-0">
              <thead>
                <tr>
                  <th>Código</th>
                  <th>Cliente</th>
                  <th>Valor</th>
                  <th>Método</th>
                </tr>
              </thead>
              <tbody>
                {{#each ultimosPagos}}
                <tr>
                  <td><code>{{this.codigo_barras}}</code></td>
                  <td>{{this.nombre_cliente}}</td>
                  <td><strong class="text-success">${{this.valor_factura}}</strong></td>
                  <td>{{this.metodo_pago}}</td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
        {{else}}
          <div class="text-center py-4">
            <i class="fas fa-info-circle text-info fa-3x mb-3"></i>
            <p class="text-muted mb-0">No hay pagos recientes</p>
          </div>
        {{/if}}
      </div>
    </div>
  </div>

  <!-- Últimas Entregas -->
  <div class="col-md-6">
    <div class="card shadow-sm h-100">
      <div class="card-header">
        <h6 class="mb-0">
          <i class="fas fa-hand-holding text-info me-2"></i>
          Últimas Entregas
        </h6>
      </div>
      <div class="card-body p-0">
        {{#if ultimasEntregas.length}}
          <div class="table-responsive">
            <table class="table table-hover mb-0">
              <thead>
                <tr>
                  <th>Código</th>
                  <th>Cliente</th>
                  <th>Tipo</th>
                  <th>Matrizador</th>
                </tr>
              </thead>
              <tbody>
                {{#each ultimasEntregas}}
                <tr>
                  <td><code>{{this.codigo_barras}}</code></td>
                  <td>{{this.nombre_cliente}}</td>
                  <td>{{this.tipo_documento}}</td>
                  <td>{{this.matrizador.nombre}}</td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
        {{else}}
          <div class="text-center py-4">
            <i class="fas fa-info-circle text-info fa-3x mb-3"></i>
            <p class="text-muted mb-0">No hay entregas recientes</p>
          </div>
        {{/if}}
      </div>
    </div>
  </div>
</div>

<!-- 🔄 AUTO-ACTUALIZACIÓN Y JAVASCRIPT INTERACTIVO -->
<script>
document.addEventListener('DOMContentLoaded', function() {
  // ============ FILTROS RÁPIDOS INTERACTIVOS ============
  document.querySelectorAll('[data-periodo]').forEach(btn => {
    btn.addEventListener('click', function() {
      const periodo = this.getAttribute('data-periodo');
      
      // Actualizar select
      document.getElementById('tipoPeriodo').value = periodo;
      
      // Destacar botón activo
      document.querySelectorAll('[data-periodo]').forEach(b => {
        b.classList.remove('active', 'btn-primary');
        b.classList.add('btn-outline-primary');
      });
      this.classList.remove('btn-outline-primary');
      this.classList.add('active', 'btn-primary');
      
      // Ocultar fechas personalizadas
      document.getElementById('fechasPersonalizadas').style.display = 'none';
      
      // Actualizar indicador de período
      actualizarIndicadorPeriodo(periodo);
      
      // Enviar formulario automáticamente
      document.getElementById('periodoForm').submit();
    });
  });
  
  // ============ MANEJO DEL SELECT DE PERÍODO ============
  document.getElementById('tipoPeriodo').addEventListener('change', function() {
    const valor = this.value;
    const fechasPersonalizadas = document.getElementById('fechasPersonalizadas');
    
    if (valor === 'personalizado') {
      fechasPersonalizadas.style.display = 'block';
    } else {
      fechasPersonalizadas.style.display = 'none';
      
      // Actualizar botones rápidos
      document.querySelectorAll('[data-periodo]').forEach(b => {
        b.classList.remove('active', 'btn-primary');
        b.classList.add('btn-outline-primary');
      });
      
      const botonCorrespondiente = document.querySelector(`[data-periodo="${valor}"]`);
      if (botonCorrespondiente) {
        botonCorrespondiente.classList.remove('btn-outline-primary');
        botonCorrespondiente.classList.add('active', 'btn-primary');
      }
    }
  });
  
  // ============ FUNCIÓN PARA ACTUALIZAR INDICADOR ============
  function actualizarIndicadorPeriodo(periodo) {
    const indicador = document.getElementById('periodoActivo');
    const hoy = new Date();
    let texto = '';
    
    switch(periodo) {
      case 'hoy':
        texto = 'HOY - ' + hoy.toLocaleDateString('es-EC');
        break;
      case 'ayer':
        const ayer = new Date(hoy);
        ayer.setDate(ayer.getDate() - 1);
        texto = 'AYER - ' + ayer.toLocaleDateString('es-EC');
        break;
      case 'semana':
        texto = 'ESTA SEMANA';
        break;
      case 'mes':
        texto = 'ESTE MES';
        break;
      case 'ultimo_mes':
        texto = 'ÚLTIMOS 30 DÍAS';
        break;
      default:
        texto = periodo.toUpperCase();
    }
    
    if (indicador) {
      indicador.textContent = texto;
    }
  }
  
  // ============ AUTO-ACTUALIZACIÓN INTELIGENTE ============
  {{#if periodo.esHoy}}
  // Solo auto-actualizar si estamos viendo "hoy"
  setInterval(function() {
    // Recargar página cada 5 minutos para datos frescos
    window.location.reload();
  }, 5 * 60 * 1000);
  {{/if}}
  
  // ============ ANIMACIONES SUAVES PARA CARDS ============
  document.querySelectorAll('.card').forEach(card => {
    card.addEventListener('mouseenter', function() {
      this.style.transform = 'translateY(-2px)';
      this.style.transition = 'transform 0.2s ease';
    });
    
    card.addEventListener('mouseleave', function() {
      this.style.transform = 'translateY(0)';
    });
  });
  
  // ============ ESTABLECER BOTÓN ACTIVO INICIAL ============
  const periodoActual = '{{periodo.rango}}';
  const botonActual = document.querySelector(`[data-periodo="${periodoActual}"]`);
  if (botonActual) {
    botonActual.classList.remove('btn-outline-primary');
    botonActual.classList.add('active', 'btn-primary');
  }
});
</script>

<!-- 🎨 ESTILOS MODERNIZADOS -->
<style>
/* Estilos base para el dashboard modernizado */
.border-left-primary {
  border-left: 0.25rem solid #4e73df !important;
}

.border-left-success {
  border-left: 0.25rem solid #1cc88a !important;
}

.border-left-warning {
  border-left: 0.25rem solid #f6c23e !important;
}

.border-left-danger {
  border-left: 0.25rem solid #e74a3b !important;
}

.border-left-info {
  border-left: 0.25rem solid #36b9cc !important;
}

/* Estilos para cards modernizadas */
.card {
  box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15) !important;
  border: 1px solid #e3e6f0;
  transition: all 0.3s ease;
}

.card:hover {
  transform: translateY(-2px);
  box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.15) !important;
}

/* Estilos para métricas */
.text-xs {
  font-size: 0.7rem;
}

.font-weight-bold {
  font-weight: 700 !important;
}

.text-uppercase {
  text-transform: uppercase !important;
}

.text-gray-800 {
  color: #5a5c69 !important;
}

.text-gray-300 {
  color: #dddfeb !important;
}

/* Estilos para alertas */
.alert {
  border: 1px solid transparent;
  border-radius: 0.35rem;
}

.alert.border-left-primary {
  border-left-color: #4e73df !important;
  background-color: rgba(78, 115, 223, 0.1);
}

.alert.border-left-success {
  border-left-color: #1cc88a !important;
  background-color: rgba(28, 200, 138, 0.1);
}

.alert.border-left-warning {
  border-left-color: #f6c23e !important;
  background-color: rgba(246, 194, 62, 0.1);
}

.alert.border-left-danger {
  border-left-color: #e74a3b !important;
  background-color: rgba(231, 74, 59, 0.1);
}

.alert.border-left-info {
  border-left-color: #36b9cc !important;
  background-color: rgba(54, 185, 204, 0.1);
}

/* Estilos para botones */
.btn {
  transition: all 0.2s ease;
}

.btn:hover {
  transform: translateY(-1px);
}

.btn-group .btn.active {
  background-color: #4e73df;
  border-color: #4e73df;
  color: white;
}

/* Estilos para tablas */
.table-responsive {
  border-radius: 0.35rem;
}

.table th {
  border-top: none;
  font-weight: 600;
  text-transform: uppercase;
  font-size: 0.8rem;
  color: #5a5c69;
}

/* Estilos para accesos rápidos */
.btn-lg {
  padding: 1.5rem 1rem;
  border-radius: 0.35rem;
}

/* Responsive */
@media (max-width: 768px) {
  .card {
    margin-bottom: 1rem;
  }
  
  .table-responsive {
    font-size: 0.875rem;
  }
  
  .btn-group {
    flex-direction: column;
  }
  
  .btn-group .btn {
    border-radius: 0.25rem !important;
    margin-bottom: 0.25rem;
  }
}

/* Animaciones */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.card {
  animation: fadeIn 0.5s ease-out;
}

/* Efectos de carga */
.loading {
  opacity: 0.6;
  pointer-events: none;
}

.loading::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 20px;
  height: 20px;
  margin: -10px 0 0 -10px;
  border: 2px solid #f3f3f3;
  border-top: 2px solid #4e73df;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
</style> 